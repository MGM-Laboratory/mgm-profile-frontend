# Normalize line endings to LF in the repo and on checkout, so local tooling
# (Prettier, ESLint) matches Linux CI regardless of host OS.
* text=auto eol=lf

# Binary / generated assets — leave untouched.
*.svg binary
*.ico binary
*.png binary
*.jpg binary
*.webp binary
*.woff binary
*.woff2 binary
